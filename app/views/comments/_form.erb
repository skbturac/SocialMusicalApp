
<!-- I think we should do a nested form to allow for someone to write their name? Either whatever they want or a username, bc we can't ask a user for their ID -->

<%= form_for [@post, @comment] do |f| %>

      <%= f.text_area :text, placeholder: "Compose new comment..." %>
<br>
    <%= f.submit "Post", class: "btn btn-primary" %>
<br>
<% end %>
